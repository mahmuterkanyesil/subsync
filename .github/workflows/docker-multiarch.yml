name: Build and push multi-arch Docker image

on:
  push:
    branches: [ main ]
  release:
    types: [ published ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      REGISTRY_HOST: ${{ secrets.REGISTRY_HOST != '' && secrets.REGISTRY_HOST || 'docker.io' }}
      REGISTRY_IMAGE: ${{ secrets.REGISTRY_IMAGE != '' && secrets.REGISTRY_IMAGE || format('{0}/subsync', secrets.REGISTRY_USERNAME) }}
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY_HOST }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build and push multi-arch image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          tags: |
            ${{ env.REGISTRY_IMAGE }}:latest
            ${{ env.REGISTRY_IMAGE }}:${{ github.sha }}
          file: ./Dockerfile

      - name: Create release tag alias
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          echo "Pushed image for tag ${GITHUB_REF}" 
