services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    volumes:
      - redis_data:/data

  agent:
    build: .
    restart: unless-stopped
    command: ./bin/agent
    environment:
      - REDIS_URL=redis://redis:6379/0
      - STATE_DB_PATH=/data/state.db
      - PROGRESS_DIR=/data/progress
      - WATCH_DIRS=${WATCH_DIRS:-/tv:/movies}
      - SCAN_INTERVAL_SEC=30
      - LOG_FORWARD_URL=http://api:8080/api/internal/logs
      - LOG_LEVEL=debug
    volumes:
      - ${DATA_DIR:-./data}:/data
      - ${TV_DIR:-/tmp/tv}:/tv:ro
      - ${MOVIES_DIR:-/tmp/movies}:/movies:ro
    depends_on:
      - redis

  worker:
    build: .
    restart: unless-stopped
    command: ./bin/worker
    environment:
      - REDIS_URL=redis://redis:6379/0
      - STATE_DB_PATH=/data/state.db
      - PROGRESS_DIR=/data/progress
      - BATCH_SIZE=500
      - WORKER_CONCURRENCY=2
      - LOG_FORWARD_URL=http://api:8080/api/internal/logs
      - LOG_LEVEL=debug
    volumes:
      - ${DATA_DIR:-./data}:/data
      - ${TV_DIR:-/tmp/tv}:/tv:ro
      - ${MOVIES_DIR:-/tmp/movies}:/movies:ro
    depends_on:
      - redis

  embedder:
    build: .
    restart: unless-stopped
    command: ./bin/embedder
    environment:
      - STATE_DB_PATH=/data/state.db
      - PROGRESS_DIR=/data/progress
      - EMBED_INTERVAL_SEC=600
      - LOG_FORWARD_URL=http://api:8080/api/internal/logs
      - LOG_LEVEL=debug
    volumes:
      - ${DATA_DIR:-./data}:/data
      - ${TV_DIR:-/tmp/tv}:/tv
      - ${MOVIES_DIR:-/tmp/movies}:/movies
    depends_on:
      - redis

  api:
    build: .
    restart: unless-stopped
    command: ./bin/api
    environment:
      - STATE_DB_PATH=/data/state.db
      - PROGRESS_DIR=/data/progress
      - REDIS_URL=redis://redis:6379/0
      - API_PORT=8080
      - LOG_LEVEL=debug
    ports:
      - "8080:8080"
    volumes:
      - ${DATA_DIR:-./data}:/data
    depends_on:
      - redis

volumes:
  redis_data:
